services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fey
      POSTGRES_PASSWORD: fey
      POSTGRES_DB: fey
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
